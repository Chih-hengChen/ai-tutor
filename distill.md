# 章节蒸馏工作流（Distillation）

本文件为 ai-tutor 的统一蒸馏流程。**所有模式（知识 / 项目 / 源码阅读 / 深度讲解）在每一章/节/主题块通过考核后必须读取本文件并执行**，把长讲解压缩成可复习的"入口"结构。

## 核心原则

> 大脑的工作方式是联想提取，不是全文检索。一个"入口"被激活后能重建 80% 的相关知识。蒸馏的目标不是压缩信息密度，而是提炼能触发回忆的最小结构。

## 触发时机

每一章 / 节点 / 主题块的考核通过、即将进入下一章之前，**立即执行**：

1. 把考核题、用户答案、LLM 点评、补充对话追加到 `raw-document/topic{N}-{slug}.md` 讲解原文之后
2. 调用本文件做蒸馏，输出到 `distilled/topic{N}-{slug}.md`

知识 / 项目 / 源码阅读模式的"节点"=1 个最小知识点；深度讲解模式的"节点"=1 个主题块（5-8 子节统一考核）。

## 蒸馏输入

LLM 自行回看本节刚刚讲解 + 考核的完整内容（已在上下文里），不需要再读 raw-document 文件。输入包括：
- 讲解阶段产生的核心概念、机制、源码引用
- 考核阶段用户的回答（哪里答对、哪里答错、错误类型）
- LLM 给出的提示和纠正

## 蒸馏输出格式（严格四段入口结构）

每个节点 / 主题块输出一个 markdown 文件 `distilled/topic{N}-{slug}.md`，结构如下，**总字数控制在 400 字以内**：

```markdown
# [节点 / 主题块名]

**它解决什么问题**
（1 句话，从痛点出发，不是定义）

**核心机制**
- （3-5 条，每条不超过 20 字，动词开头）
- ...
- ...

**最容易被追问的点**
- "[反直觉问题]？" → [一句话解释]
- "[高频混淆点]？" → [一句话解释]
- "[边界情况]？" → [一句话解释]

**一句话类比**
（用日常已懂的东西打比方，结构对应而非表面相似）
```

## 写作约束

1. **不允许超字数**：超过 400 字必须删减，宁可少写一条机制，不可铺展。
2. **不允许罗列定义**：每一条都必须是"动作 + 对象 + 结果"，禁止"X 是 Y"的静态陈述。
3. **机制条数 3-5 条**：少于 3 条说明理解不够；多于 5 条说明没分清主次。
4. **追问点必须反直觉**：不要写教科书目录式条目，写"面试官最爱问、考生最常错"的点。
5. **类比要结构对应**：类比中的要素必须能在机制里找到真实映射（如"Fiber 像抄书时每段检查有没有更急的事"，"抄到哪了"=workInProgress 树）。
6. **基于实际讲解内容**：不可编造本节没讲过的概念。如果考核中暴露的盲区值得收录，写到"最容易被追问的点"。

## 考核盲区反馈到蒸馏

如果用户考核时犯了**概念性错误**或**高频混淆**，强制把这个点收入"最容易被追问的点"。这是蒸馏区别于普通总结的核心价值——它记下了"用户实际会犯的错"，而不只是"教材认为重要的点"。

## 文件路径约定

- 输出目录：`./ai-tutor/distilled/`
- 文件名：`topic{N}-{slug}.md`，与 `raw-document/topic{N}-{slug}.md` 一一对应
- 总览索引（可选）：当一个主题的所有节点都蒸馏完毕，在 `./ai-tutor/distilled/{slug}-overview.md` 中合并所有蒸馏条目，作为该主题的"面试前一晚默想文档"

## 与 raw-document 的分工

| 文件 | 内容 | 用途 |
|------|------|------|
| `raw-document/topic{N}-{slug}.md` | 完整讲解原文 + 考核过程 | 深挖细节、回忆不起来时回看 |
| `distilled/topic{N}-{slug}.md` | 400 字入口结构 | 高频复习、强迫提取、面试默想 |

**复习方法（必须告知用户）**：看 `distilled/` 的标题和"它解决什么问题"后**合上文档默想**剩余内容，想不出来再翻 `raw-document/`。每次复习都在强迫提取，而不是重新阅读。

## 降级与边界

- **讲解本身已极短**（如纯定义性知识点）：蒸馏可缩到 200 字，但四段结构不可省略。
- **主题块涉及多机制交织**（如深度讲解模式）：可拆为 2 个蒸馏文件（topic{N}a、topic{N}b），每个独立 400 字。
- **考核未通过但有部分掌握**：仍执行蒸馏，但在最容易被追问点里多记一两条用户答错的真实盲区。
