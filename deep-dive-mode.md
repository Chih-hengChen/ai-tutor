# 深度讲解模式工作流

本文件为深度讲解模式的详细工作流。当 SKILL.md 判定进入深度讲解模式后，读取本文件执行。

## 适用场景

需要理解复杂系统内部机制的主题，特征：

- 涉及大型源码库（React 源码、Vue 源码、Webpack 源码等）
- 运行时/引擎级原理（V8 引擎、JVM、Node.js 事件循环等）
- 编译器/解释器原理（Babel、TypeScript 编译器、LLVM 等）
- 操作系统内核机制（进程调度、内存管理、文件系统等）
- 任何以"源码级理解"、"底层原理"、"深入实现机制"为目标的学习

用户明确表达"深入理解"、"源码级"、"底层原理"、"实现机制"时触发。SKILL.md 在模式识别阶段发现主题属于系统级底层内容时也应路由到此模式。

## 与知识模式的核心区别

知识模式适合"学会用"（React Hooks 怎么用），深度模式适合"理解为什么这样设计"（React Hooks 的 Fiber 链表是怎么工作的）。

| 维度     | 知识模式             | 深度讲解模式                 |
| -------- | -------------------- | ---------------------------- |
| 粒度     | 最小知识点           | 最小可闭环机制               |
| 单节深度 | 精炼，控制篇幅       | 讲透，允许长输出             |
| 考核时机 | 每个知识点后立刻考核 | 一个主题块讲完后统一考核     |
| 源码引用 | 可选                 | 必须（文件名、函数名、行号） |
| 可视化   | 辅助说明             | 每节必须有结构图或流程图     |

## 大纲策略：主题块拆分

不按"知识点"拆分，按"完整机制"拆分为**主题块**。每个主题块围绕一个核心问题展开，包含 5-8 个连续子节。

### 拆分原则

每个主题块必须是一个**可独立理解的完整闭环**：

- 有明确的"它解决什么问题"
- 有核心数据结构和关键字段
- 有完整的运行流程
- 能和前后主题块建立关联

### 拆分示例（React Fiber 架构）

不要把 "Fiber 架构" 当成一个小节。应该这样拆：

```
主题块: Fiber 架构
  1.1 Fiber 为什么出现 — Stack Reconciler 的瓶颈在哪
  1.2 FiberNode 的核心字段 — tag、type、return、child、sibling 等
  1.3 双缓存树 — current 树与 workInProgress 树的关系
  1.4 render 阶段如何遍历 — beginWork 和 completeWork 的交替
  1.5 commit 阶段如何提交 — mutation、layout、passive 三个子阶段
  1.6 调度和优先级如何接入 — lane 模型与 Scheduler 的交互

主题块: Hooks 实现
  2.1 Hooks 链表结构 — memoizedState 如何串联
  2.2 useState 的 mount 和 update — mountState vs updateState
  2.3 useEffect 的注册和执行 — mountEffect vs updateEffect
  ...
```

### 拆分示例（V8 引擎）

```
主题块: JavaScript 执行流水线
  1.1 从源码到字节码 — Parser → BytecodeGenerator
  1.2 Ignition 解释器 — 字节码执行主循环
  1.3 TurboFan 编译管线 — 热点检测到机器码生成
  1.4 内联缓存 (IC) — 如何加速属性访问
  1.5 隐藏类 — 对象形状与内存布局优化
```

## Phase 1: 摸底与规划

1. **恢复检测：** 检查 `./ai-tutor/records/` 下未完成的深度讲解模式记录。
2. **确认学习深度：** 询问用户期望的深度：
   - "理解设计思想和核心机制"（不需要追踪每一行代码）
   - "能跟着源码读下去"（需要具体的文件、函数、行号指引）
   - 根据回答调整引用源码的详细程度
3. **生成架构总览图：** 用 Mermaid 或 ASCII 画出整个系统的架构鸟瞰图，标注各子系统/模块的关系和数据流向。
4. **生成主题块大纲：** 按上述拆分原则，生成主题块和子节的大纲。每个子节标注一个核心问题。
5. **展示确认：** 让用户选择先学哪个主题块，允许调整范围。
6. **写入文件：** 按深度讲解模式模板写入记录。

> **前置知识检测（惰性触发）：** 不在启动阶段集中检测，而是在第一个主题块正式开始前出一道诊断题。前置不足 → 建议补基础，但允许跳过；后续涉及该前置时自动补充内联解释。

### 深度讲解模式记录模板

```markdown
---
mode: deep-dive
start_date: YYYY-MM-DD
target_depth: conceptual | source-code
source_version: "[框架名]@[版本号]"
nodes:
  "1.1":
    name: [子节名]
    module: [主题块名]
    status: pending
    attempts: 0
    last_tested: null
    mastery_level: 0
    core_question: [这节要回答的核心问题]
---

# [名称] 学习记录

## 学习日志

| 日期 | 节点 | 结果 | 备注 |
| ---- | ---- | ---- | ---- |
```

## Phase 2: 深度讲解

这是深度模式的核心，每节必须按固定结构展开，不允许压缩成简短概述。

### 单节讲解结构（6 段式，每节必须全部包含）

**第 0 段：直觉锚定（在进入机制之前，先建立可挂载后续知识的心智模型）**

在介绍任何概念之前，先用一个具体的、可视化的类比或最小场景，让用户对这个机制有一个直觉感知。

直觉锚定的三条硬性要求：

- **必须是结构对应的类比**，不能是表面相似的假类比。类比中的每个要素必须能在后续机制中找到真实对应物
- **在后续讲解中主动回调**：每当引入一个核心概念时，用括号或短句把它和直觉锚定的对应物关联起来（"这里的 lane，就是我们说的优先通道"）
- **不要过度类比**：如果某个机制本身并不复杂，跳过此段，直接进入第 1 段

**示例（差）：**

> "React Fiber 就像一个可以暂停的任务队列。"——类比过于抽象，队列中的"任务"对应什么、"暂停"如何实现都没有结构对应。

**示例（好）：**

> "想象你在手工抄写一本书。旧方案是：拿到书就必须一口气抄完才能放下笔，中途不能被打断。Fiber 要解决的是：每抄完一段，先检查有没有更紧急的事，有就先去处理，然后回来接着抄。这里'抄到哪了'就是 workInProgress 树；'回来接着抄'需要的寻路能力，就是 return / child / sibling 三个指针。"

---

**第 1 段：问题背景（这个机制解决什么问题）**

- 旧方案/旧实现有什么缺陷
- 新方案要达到什么目标
- 为什么现有机制不够用

**注意：** 在引入"几乎所有人第一次都会想错"的概念时，在这里提前拦截，不要等到第 5 段再纠正。格式如下：

> ⚠️ **常见先入为主的误解：** 很多人会以为 [错误理解]，实际上是 [正确理解]，因为 [原因]。带着这个理解往下看。

---

**第 2 段：核心数据结构（涉及哪些关键对象/字段）**

- 列出核心数据结构，用 ASCII 或表格展示字段布局
- **只画本节涉及的字段**，其余字段用 `...` 省略，避免信息过载
- 标注每个字段的作用，以及字段之间的关系（指针、引用、索引）
- 如果源码中有 TypeScript/Flow 类型定义，引用真实类型
- 涉及链表、树、指针时，**必须画出至少 3 个节点的实例**，不能只画单节点加省略号；箭头必须有语义标注（写明"指向"、"引用"、"触发"还是"返回"，不能只是连线）

---

**第 3 段：运行流程（代码如何推进）**

流程图类型选择规则：

- 函数调用链 → 竖向时序图
- 状态变化 → 横向状态机图
- 不要混用两种图

流程图中的每个节点写函数名，禁止写"处理逻辑"、"执行操作"等占位词。分支条件必须标注在箭头上，不能只画菱形框。

源码引入使用以下四步格式（不能只甩一个函数名）：

```
1. 定位：[框架名]@[版本号] · [文件路径] · [函数名]
2. 签名解读：解释每个参数是什么，返回值代表什么
3. 阅读入口：告诉用户"先看第 N 行到第 M 行，忽略其他部分"
4. 关键行标注：明确指出哪一行或哪几行是本节要理解的核心
```

**示例：**

> 这个逻辑在 `react@18.2.0` 的 `ReactFiberBeginWork.js` 中，函数是 `updateFunctionComponent(current, workInProgress, Component, nextProps, renderLanes)`。参数里 `current` 是上次渲染的 Fiber，`workInProgress` 是本次要构建的 Fiber。打开文件后，先看第 702–730 行，其他部分暂时跳过。核心是第 718 行的 `renderWithHooks` 调用——这一行触发了用户写的函数组件本体，也是 Hooks 链表被实际读写的地方。

伪代码剪裁规则（必须遵守）：

```
必须保留：
- 所有影响控制流的条件分支（if/switch 的每个分支都要出现）
- 所有涉及本节核心数据结构的读写操作
- 递归或循环的终止条件
- 与上下游子节有接口关系的函数调用

可以折叠（写成一行注释）：
- 错误处理、日志、开发模式警告
- 与本节机制无关的优化路径（注明存在但不展开）
- 具体的位运算细节（说明"用位运算做标记"即可）

禁止：
- 把多个函数的逻辑合并成一个伪函数（破坏源码对照能力）
- 用"处理 xxx"代替真实的操作名称
- 把核心判断简化成"// 核心逻辑"
```

伪代码必须和本节的流程图对应：图里的每个框，在伪代码里都应该能找到对应的行。

---

**第 4 段：设计动机与权衡（为什么这样设计）**

- 这个设计要优化的核心约束是什么（性能 / 内存 / 可中断性 / 简单性 / …）
- 如果存在旧方案：与旧方案对比，说明差异和设计动机
- 如果没有旧方案：说明为什么这个设计是该约束下的合理解，有哪些备选方案被放弃了
- 这个设计牺牲了什么（复杂度、内存占用、灵活性等）
- 与同一系统中其他机制的关系

---

**第 5 段：次级误解和边界（需要先理解完整机制才能辨别的坑）**

> **注意与第 1 段的分工：** 第 1 段拦截"高危误解"（几乎所有人一开始都会犯的先入为主错误）。第 5 段处理"次级误解"——那些只有在理解完整机制之后才会产生，或者需要完整上下文才能说清为什么错的误解。如果一个误解可以在第 1 段就拦截，不要留到第 5 段。

- 列出 1-3 个次级误解，说明为什么错
- 标注边界条件和特殊场景（错误处理、并发、性能边界等）

---

### 子节之间的认知交接

同一主题块内的子节连续讲解，不要在每节后都停下来考核。每个子节结束时，**必须用以下句式做显式交接**，不能只用"接下来我们看……"随意过渡：

```
交接句式：
"现在我们知道了 [本节核心结论，一句话]。
但这里有一个问题还没有回答：[下一节要解决的核心问题]。
这就是 [下一节标题] 要处理的事情。"
```

**示例：**

> "现在我们知道了 FiberNode 通过 return、child、sibling 三个指针构成了一棵链表树，每个节点都能向上找到父节点、向下找到第一个子节点。但这棵树具体是怎么被遍历的——React 是深度优先还是广度优先，遍历过程中做了什么——这是 beginWork 和 completeWork 要回答的问题。"

这个句式要求每节结尾必须想清楚"本节的输出是什么、下一节的输入是什么"，确保主题块内部形成真正的认知链条，而不是孤立的子节堆叠。

一个主题块全部讲完后，统一进入 Phase 3 考核。

### 子节讲解直接写入 raw-document（不在终端输出讲解正文）

深度模式的讲解正文**必须直接用 Write 工具写入 `./ai-tutor/raw-document/topic{N}-{slug}.md`，终端不打印讲解正文**。用户读 md 文件而不是看终端。

- 每个主题块对应一个 raw-document 文件，**主题块内的所有子节共用同一文件**
- 第一个子节开始时新建文件并写入头部：
  ```markdown
  # [主题块名]

  ## 讲解原文

  ### 子节 1.N：[子节标题]

  [本子节 6 段式完整讲解]
  ```
- 后续子节追加到 `## 讲解原文` 段下方（用 `### 子节 N.M` 作为分隔），保持 `## 考核过程` 段为空待 Phase 3 填充
- 每写完一个子节，终端只输出短摘要：
  ```
  ✅ 已写入子节 1.N：[子节标题] → ./ai-tutor/raw-document/topic{1}-{slug}.md
  📌 [本子节核心问题的一句话回答 + 1-3 条关键结论]
  ```
- `visual_tool: html` 时可视化 HTML 文件仍生成到 `./ai-tutor/visuals/`，并在 raw-document 中标注链接

## Phase 3: 主题块考核

一个主题块（5-8 个子节）全部讲完后，进行统一考核。

### 考核形式

出 2-3 道题，覆盖该主题块的核心机制：

1. **源码追踪题**（必须）：给出一个具体场景，让用户追踪调用链
   - "当 React 执行到 `beginWork(current, workInProgress, renderLanes)` 时，如果 workInProgress.tag 是 FunctionComponent，接下来会走哪个函数？整个调用链是什么？"
2. **机制推理题**（必须）：改变某个条件，让用户推理结果
   - "如果把 Fiber 的 return 指针去掉，会导致什么问题？在哪个具体环节会出错？"
3. **设计理解题**（可选）：关于设计决策的理解
   - "React 为什么选择用链表而不是数组来组织 Fiber 节点？如果用数组会有什么问题？"

### 评分标准

每道题独立评分，不做整体模糊判断：

| 题型       | 必须项（缺一不通过）               | 加分项                 |
| ---------- | ---------------------------------- | ---------------------- |
| 源码追踪题 | 关键函数节点全部提到；调用顺序正确 | 提及边界分支或条件判断 |
| 机制推理题 | 说出"哪个环节出错"；说出"为什么"   | 能指出具体源码位置     |
| 设计理解题 | 答出核心权衡                       | 能举具体反例或备选方案 |

**通过标准：** 所有必须项达到 → 该题通过。加分项不影响通过，但记入学习日志。

### 评分与后续

- 全部题目通过 → 主题块通过，执行下方"通过后四步"
- 部分题目通过 → 通过题目对应的子节标记为 mastered，未通过的子节补充讲解（重新写一份子节段追加到 raw-document），然后单独出题；最终全过后执行下方"通过后四步"
- 全部未通过 → 回到该主题块，用不同角度重新讲解核心机制（重新写一份 `### 子节 N.M（重讲）` 追加段），然后重新考核

### 通过后四步（主题块全部通过时执行）

**(a) 更新记录文件**
将主题块内所有子节状态置为 `mastered`、`last_tested: 今天`、`mastery_level: 1`。学习日志追加到 Markdown 正文。

**(b) 追加考核过程到 raw-document**
Read `./ai-tutor/raw-document/topic{N}-{slug}.md`，在文件末尾的 `## 考核过程` 段下追加：
```markdown
## 考核过程

### 题目 1：[源码追踪题题干]

**用户回答**：
[要点摘要]

**点评**：
[评估 + 通过/未通过 + 错误类型]

### 题目 2：[机制推理题题干]
...

### 题目 3（如有）：[设计理解题题干]
...

### 补充对话（如有提示/纠正）
[关键几轮要点]

---
通过时间：YYYY-MM-DD
```

**(c) 立即蒸馏本主题块**
Read `distill.md` 工作流文件，按其四段入口结构生成 `./ai-tutor/distilled/topic{N}-{slug}.md`（≤400 字）。**主题块涉及多机制时**可拆为 `topic{N}a-{slug}.md`、`topic{N}b-{slug}.md` 等多个蒸馏文件，每个独立 400 字。**考核中用户答错的点必须收入"最容易被追问的点"**。

**(d) 终端提示用户**
```
✅ 主题块 [N] 已通过
📄 raw-document: ./ai-tutor/raw-document/topic{N}-{slug}.md（含全部子节讲解 + 本次考核过程）
🎯 distilled :  ./ai-tutor/distilled/topic{N}-{slug}.md（400 字复习版）
```
然后进入 Phase 4 主题块总结。

## Phase 4: 主题块总结

一个主题块通过后，生成深度总结文档：

1. **机制总结图**：用一张完整的 Mermaid 或 ASCII 图把该主题块所有子节的机制串联起来，展示端到端的数据流和控制流
2. **源码地图**：列出该主题块涉及的所有源码文件和关键函数，格式为 `[框架名]@[版本号] · [文件路径] · [函数名]`，作为用户后续自行翻阅的索引
3. **设计模式归纳**：总结源码中体现的设计模式或架构思想
4. **跨主题块关联**：标注这个主题块和其他主题块的接口点（"这个模块的输出是下一个模块的输入"）
5. **写入文件：** `./ai-tutor/summaries/[主题slug]_[主题块slug].md`

完成一个主题块后，回到总览图标注已完成区域，让用户选择下一个主题块。

## 深度讲解模式的特殊约束

1. **以讲透为优先**：不要因为"输出克制"而压缩讲解。宁可一节讲 2000 字把机制说透，也不要 500 字带过然后做题。
2. **源码级精度**：引用的函数名、文件路径、字段名必须准确，并标注版本号。如果不确定具体位置，如实告诉用户"这个函数在 xxx 模块中，具体行号可能因版本不同"，不要编造。
3. **机制闭环**：每个子节必须形成"直觉→问题→结构→流程→动机→边界"的完整闭环，不能只讲一半。
4. **伪代码不是偷懒**：伪代码是帮助用户建立框架认知的工具，核心分支和关键判断必须保留，严格遵守剪裁规则。
5. **鼓励用户对照源码**：讲解时主动提示"你可以打开 xxx.js 跟着看"，培养用户读源码的能力。
6. **误解要分层处理**：在第 1 段提前拦截高危误解，不要把所有误解都堆在第 5 段。
